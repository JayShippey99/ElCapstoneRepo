using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PianoRoll : MonoBehaviour
{
    // this script will get the script and when it submits or something it will place notes based on
    // lets bring this script to the submit, OR we add the extra script just for this puzzle?
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
