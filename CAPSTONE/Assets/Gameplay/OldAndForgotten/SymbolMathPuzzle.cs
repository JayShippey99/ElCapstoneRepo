using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SymbolMathPuzzle : MonoBehaviour
{
    // this puzzle idea sucks if all it is is you put in things and then they are documented and then boom that's it you just have that reference
    // what if up with the frequencies
    // WAIT the thing gave us frequencies, tht's what it was. OMG what if it gives us a frequency for an equation, yeah it think that's what the puzzle was
    // so we need a place to send us that information
    // is that still just easy as hell

    
    //public SymbolChanger sc;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void ChangeSymbols(string t)
    {
        //sc.StartSymbolSwitch(t); // weird stupid way of doing this but it works
    }
}
